<script lang="ts">
  // Empty on purpose - the pane exists to prove the unlock (dialog choice ->
  // effect -> feature -> nav item) end-to-end. The pet's own state, stats,
  // and whatever it actually does live with a real follow-up.
</script>

<p class="empty">It's around here somewhere.</p>

<style>
  .empty {
    font-family: var(--font-body);
    font-style: italic;
    color: var(--ink-faint);
    margin: 0;
  }
</style>
